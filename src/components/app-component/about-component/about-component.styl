@import "../../../common/common.styl";

.about-component {
    display: flex;
    align-items: center;
    margin: 0 auto;
    width: 700px;

    &__photo {
        border-radius: 50%;
        border: 1px solid $colors.gray;
        margin-right: 20px;
        height: 200px;
        width: 200px;
    }
}